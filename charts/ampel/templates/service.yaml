apiVersion: v1
kind: Service
metadata:
  name: {{ include "ampel.fullname" . }}
  labels:
    {{- include "ampel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ampel.selectorLabels" . | nindent 4 }}
